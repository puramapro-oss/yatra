# YATRA — ERRORS.md
> Registre des bugs rencontrés et résolus. Format : `|DATE|BUG|CAUSE|FIX|`

| Date | Bug | Cause | Fix |
|---|---|---|---|
| 2026-08-15 | TypeScript erreur `Type 'unknown' is not assignable to type 'ReactNode'` dans NotificationList.tsx ligne 70 | Condition de rendu utilisant `n.data?.group_id` (type unknown) directement, TypeScript strict mode refuse | Remplacement condition par `n.data?.group_id !== undefined` + cast `String(n.data.group_id)` dans l'href |
| 2026-08-15 | TypeScript erreur `Expected 2-3 arguments, but got 1` dans z.record() API groups/create | Zod `z.record()` nécessite 2 arguments (type clé + type valeur) depuis version récente | Ajout argument type clé : `z.record(z.string(), z.unknown())` |
| 2026-08-15 | Migration p26 : `ERROR column "read" does not exist` sur CREATE INDEX + trigger `notify_group_threshold_reached` insérait sur colonnes `body`/`data` inexistantes (plantage silencieux différé au premier seuil de pool atteint) | Table `yatra.notifications` préexistante a le schéma réel `is_read`/`message`/`link` (pas `read`/`body`/data jsonb) — l'agent P19 a supposé un schéma sans vérifier `information_schema.columns` d'abord | Fix direct VPS (`CREATE OR REPLACE FUNCTION` + index manqué) + alignement code (`route.ts`, `[id]/read/route.ts`, `NotificationList.tsx`) sur le vrai schéma. Toujours vérifier `information_schema.columns` avant d'écrire du code contre une table déjà existante, ne jamais supposer le schéma |
| 2026-08-23 | Socle légal NIYAMA : migration `supabase/migrations/p31_legal_niyama.sql` (3 tables `legal_acceptances`/`cookie_consents`/`account_deletion_requests`, schéma `yatra` dédié) NON exécutée | SSH VPS (72.62.191.111:22) `Connection refused` depuis ce sandbox — HTTPS 443 OK par ailleurs, filtrage réseau sandbox (pas un pb de credentials, cf PIEGES.md §16 entrée mukti 2026-08-23) | Migration prête à l'emploi dans `supabase/migrations/p31_legal_niyama.sql` (avec GRANT explicites, `-U supabase_admin`). À exécuter dès qu'une session avec accès VPS est dispo, puis régénérer `src/types/database.ts`. Tant que non exécutée : les 3 tables restent absentes des types générés (attendu, pas un bug — `tsc --noEmit`/`build` passent à 0 malgré ça, les nouvelles routes API `.from('legal_acceptances'\|'cookie_consents'\|'account_deletion_requests')` ne cassent pas la compilation) |
| 2026-08-23 | `next.config.ts` : `tsc --noEmit` échouait `TS1117: An object literal cannot have multiple properties with the same name` (bloquant, pré-existant, hors-scope tâche socle légal) | Travail non commité d'une session antérieure (intégration `@purama/smarana`) avait laissé 2 clés `experimental` dans le même objet `nextConfig` | Fusion des 2 blocs `experimental` en un seul (`externalDir: true` + `optimizePackageImports`), rien d'autre touché. `tsc --noEmit` et `npm run build` repassent à 0 erreur |

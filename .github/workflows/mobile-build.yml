name: Mobile Wrapper Build (Capacitor)

# Enveloppement mobile du site web live (kaia.purama.dev) — AUCUNE réécriture.
# ios/ et android/ ne sont jamais commit (gitignore) : régénérés à chaque run
# depuis capacitor.config.ts + resources/ (source unique de vérité).
# Recette générique documentée dans ~/purama/MOBILE.md — reproductible sur
# n'importe quelle app Purama en changeant juste le nom du repo/secrets.

on:
  push:
    branches: [main]
    paths:
      - 'capacitor.config.ts'
      - 'resources/**'
      - '.github/workflows/mobile-build.yml'
  workflow_dispatch: {}

jobs:
  build-android:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # Déps git privées (@purama/smarana) : le lockfile résout en git+ssh,
      # illisible sur un runner sans clé — réécriture vers https + token.
      - name: Configure git for private npm deps
        env:
          GH_PRIVATE_DEPS_TOKEN: ${{ secrets.GH_PRIVATE_DEPS_TOKEN }}
        run: git config --global url."https://x-access-token:${GH_PRIVATE_DEPS_TOKEN}@github.com/".insteadOf "ssh://git@github.com/"

      - name: Install deps
        run: npm ci --legacy-peer-deps

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Scaffold Android platform (Capacitor)
        run: |
          npx cap add android
          npx cap sync android

      - name: Generate icons + splash
        run: |
          npx @capacitor/assets generate --android --iconBackgroundColor '#0a0a0f' --iconBackgroundColorDark '#0a0a0f' --splashBackgroundColor '#0a0a0f' --splashBackgroundColorDark '#0a0a0f'

      - name: Inject release signing config
        run: |
          cat > android/app/signing.gradle << 'GRADLE_EOF'
          android {
              signingConfigs {
                  release {
                      storeFile file(System.getenv("PURAMA_KEYSTORE_PATH"))
                      storePassword System.getenv("PURAMA_KEYSTORE_PASSWORD")
                      keyAlias System.getenv("PURAMA_KEY_ALIAS")
                      keyPassword System.getenv("PURAMA_KEY_PASSWORD")
                      // v1 explicite: certains minSdk/AGP récents désactivent le schéma v1
                      // par défaut, ce qui rend l'APK invisible à jarsigner/unzip (mais
                      // valide via v2/v3). On force les deux pour rester détectable par
                      // les deux méthodes de vérif.
                      enableV1Signing true
                      enableV2Signing true
                  }
              }
              buildTypes {
                  release {
                      signingConfig signingConfigs.release
                  }
              }
          }
          GRADLE_EOF
          echo "" >> android/app/build.gradle
          echo "apply from: 'signing.gradle'" >> android/app/build.gradle

      - name: Decode keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > "$RUNNER_TEMP/purama-mobile-release.jks"

      - name: Build signed release APK
        working-directory: android
        env:
          PURAMA_KEYSTORE_PATH: ${{ runner.temp }}/purama-mobile-release.jks
          PURAMA_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          PURAMA_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          PURAMA_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: ./gradlew assembleRelease --no-daemon

      - name: Verify APK is signed
        run: |
          APK=$(find android/app/build/outputs/apk/release -name "*.apk" | head -1)
          echo "APK: $APK"
          APKSIGNER=$(find "$ANDROID_HOME/build-tools" -name apksigner | sort -V | tail -1)
          if [ -n "$APKSIGNER" ]; then
            "$APKSIGNER" verify --verbose "$APK" || (echo "APK NOT SIGNED (apksigner)" && exit 1)
          else
            unzip -l "$APK" | grep -i "META-INF/.*\.\(RSA\|SF\)" || (echo "APK NOT SIGNED (v1 fallback check)" && exit 1)
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: yatra-android-release-apk
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 30

  build-ios:
    runs-on: macos-latest
    timeout-minutes: 30
    env:
      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # Déps git privées (@purama/smarana) : le lockfile résout en git+ssh,
      # illisible sur un runner sans clé — réécriture vers https + token.
      - name: Configure git for private npm deps
        env:
          GH_PRIVATE_DEPS_TOKEN: ${{ secrets.GH_PRIVATE_DEPS_TOKEN }}
        run: git config --global url."https://x-access-token:${GH_PRIVATE_DEPS_TOKEN}@github.com/".insteadOf "ssh://git@github.com/"

      - name: Install deps
        run: npm ci --legacy-peer-deps

      - name: Scaffold iOS platform (Capacitor)
        run: |
          npx cap add ios --packagemanager CocoaPods
          npx cap sync ios

      - name: Generate icons + splash
        run: |
          npx @capacitor/assets generate --ios --iconBackgroundColor '#0a0a0f' --iconBackgroundColorDark '#0a0a0f' --splashBackgroundColor '#0a0a0f' --splashBackgroundColorDark '#0a0a0f'

      - name: Install CocoaPods dependencies
        working-directory: ios/App
        run: pod install

      - name: Compile check (unsigned — prouve que le projet Xcode compile)
        working-directory: ios/App
        run: |
          xcodebuild build \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO

      - name: Import App Store Connect API key
        if: ${{ env.ASC_KEY_ID != '' && env.APPLE_TEAM_ID != '' }}
        run: |
          mkdir -p ~/private_keys
          echo "${{ secrets.ASC_KEY_CONTENT }}" | base64 -d > ~/private_keys/AuthKey_${{ secrets.ASC_KEY_ID }}.p8

      # Certificat Apple Distribution UNIQUE partagé par toute la flotte (piège #12 MOBILE.md :
      # laisser Xcode auto-créer un certificat par app épuise le quota de comptes Apple en ~10 runs).
      # On importe ce certificat existant dans le trousseau CI AVANT l'archive : Xcode le réutilise
      # au lieu d'en créer un nouveau, seule la provisioning profile (non quotée pareil) est générée par app.
      - name: Import shared iOS Distribution certificate
        if: ${{ env.ASC_KEY_ID != '' && env.APPLE_TEAM_ID != '' }}
        env:
          IOS_DIST_CERT_P12_BASE64: ${{ secrets.IOS_DIST_CERT_P12_BASE64 }}
          IOS_DIST_CERT_PASSWORD: ${{ secrets.IOS_DIST_CERT_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/fleet-signing.keychain-db"
          KEYCHAIN_PASSWORD=$(openssl rand -base64 24)
          echo "$IOS_DIST_CERT_P12_BASE64" | base64 -d > "$RUNNER_TEMP/fleet-distribution.p12"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$RUNNER_TEMP/fleet-distribution.p12" -k "$KEYCHAIN_PATH" -P "$IOS_DIST_CERT_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security list-keychain -d user -s "$KEYCHAIN_PATH" $(security list-keychains -d user | sed 's/"//g')
          rm -f "$RUNNER_TEMP/fleet-distribution.p12"

      - name: Archive + export signed IPA (App Store Connect API key)
        if: ${{ env.ASC_KEY_ID != '' && env.APPLE_TEAM_ID != '' }}
        working-directory: ios/App
        run: |
          xcodebuild archive \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -archivePath "$RUNNER_TEMP/App.xcarchive" \
            -destination 'generic/platform=iOS' \
            -allowProvisioningUpdates \
            -authenticationKeyPath ~/private_keys/AuthKey_${{ secrets.ASC_KEY_ID }}.p8 \
            -authenticationKeyID ${{ secrets.ASC_KEY_ID }} \
            -authenticationKeyIssuerID ${{ secrets.ASC_ISSUER_ID }} \
            DEVELOPMENT_TEAM=${{ secrets.APPLE_TEAM_ID }}

          cat > "$RUNNER_TEMP/ExportOptions.plist" << 'PLIST_EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key><string>app-store-connect</string>
            <key>teamID</key><string>${{ secrets.APPLE_TEAM_ID }}</string>
            <key>signingStyle</key><string>automatic</string>
          </dict>
          </plist>
          PLIST_EOF

          xcodebuild -exportArchive \
            -archivePath "$RUNNER_TEMP/App.xcarchive" \
            -exportPath "$RUNNER_TEMP/export" \
            -exportOptionsPlist "$RUNNER_TEMP/ExportOptions.plist" \
            -allowProvisioningUpdates \
            -authenticationKeyPath ~/private_keys/AuthKey_${{ secrets.ASC_KEY_ID }}.p8 \
            -authenticationKeyID ${{ secrets.ASC_KEY_ID }} \
            -authenticationKeyIssuerID ${{ secrets.ASC_ISSUER_ID }}

      - name: Skip signed archive (secrets manquants)
        if: ${{ env.ASC_KEY_ID == '' || env.APPLE_TEAM_ID == '' }}
        run: |
          echo "::warning::Archive signée iOS sautée — APPLE_TEAM_ID manquant (secret repo) et/ou agreement App Store Connect non signé (voir MOBILE.md §Blocages)."

      - uses: actions/upload-artifact@v4
        if: ${{ env.ASC_KEY_ID != '' && env.APPLE_TEAM_ID != '' }}
        with:
          name: yatra-ios-release-ipa
          path: ${{ runner.temp }}/export/*.ipa
          retention-days: 30
